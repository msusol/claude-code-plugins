{
  "name": "clinerules",
  "version": "1.0.0",
  "description": "The /install-clinerules skill — symlinks ~/.clinerules/* into the current project root's .clinerules/ directory and updates the @-import block in ~/.claude/CLAUDE.md.",
  "author": {
    "name": "clinerules"
  }
}
