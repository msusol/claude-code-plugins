{
  "name": "git-guard",
  "version": "1.0.0",
  "description": "The /git-commit and /git-push skills — the sanctioned path through git-guard's shell wrapper + PreToolUse hook + deny rules. Each walks its own stages with explicit confirmation at every step; push always requires its own separate confirmation.",
  "author": {
    "name": "Mark Susol"
  },
  "license": "MIT"
}
