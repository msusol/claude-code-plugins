{
  "$schema": "https://anthropic.com/claude-code/marketplace.schema.json",
  "name": "git-guard",
  "description": "git-guard local marketplace — exposes the /commit skill (the sanctioned escape hatch for the shell wrapper + PreToolUse hook that block unguided git writes).",
  "owner": {
    "name": "Mark Susol"
  },
  "license": "MIT",
  "plugins": [
    {
      "name": "git-guard",
      "description": "Provides the /commit skill — the only sanctioned path through the git-guard wrapper + hook + deny rules. Walks each stage of a git commit with explicit confirmation at every step.",
      "category": "development",
      "source": "./plugins/git-guard"
    }
  ]
}
